name: PR
on:
  pull_request_target:
permissions:
  pull-requests: write
jobs:
  pr:
    name: PR
    runs-on: ubuntu-latest
    steps:
      - uses: amannn/action-semantic-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
