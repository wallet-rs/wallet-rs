{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": [
        "**",
        "../.cargo/**",
        "../crates/**",
        "../Cargo.lock",
        "../Cargo.toml",
        "../rust-toolchain.toml",
        "../rustfmt.toml"
      ],
      "outputs": []
    }
  }
}
