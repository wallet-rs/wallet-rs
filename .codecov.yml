coverage:
  status:
    patch: true
    project:
      default:
        target: auto
        threshold: 0%
        base: auto
comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: true
ignore:
  - "tools/*"
