coverage:
  status:
    patch: off
    project:
      default:
        threshold: null
        informational: true
github_checks:
  annotations: true
comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: true
ignore:
  - "tools/*"
